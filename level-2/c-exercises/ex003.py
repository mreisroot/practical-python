# Function that returns the average of a list of numbers
def compute_list_avg(number_list):
    return sum(number_list) / len(number_list)

# A list of 4 numbers
number_list = [3, 4, 5, 2]

# Printing the returned value
print(compute_list_avg(number_list))
